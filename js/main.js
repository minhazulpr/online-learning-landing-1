'use-strict';

/**
 * Swiper JS
 */

var swiper = new Swiper(".mySwiper", {
    speed: 0,
    loop:true,
    effect:"fade",
    pagination: {
        el: ".swiper-pagination",
        clickable:true
    }
});
